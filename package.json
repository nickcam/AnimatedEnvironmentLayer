{
  "version": "1.0.0",
  "name": "AnimatedEnvironmentLayer",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc",
    "server": "http-server",
    "watch:build": "tsc --watch",
    "reload": "reload -b",
    "start": "npm-run-all clean build --parallel watch:build reload --print-label"
  },
  "devDependencies": {
    "@types/arcgis-js-api": "4.21.0",
    "dojo-typings": "1.11.9",
    "npm-run-all": "^4.1.3",
    "reload": "3.2.1",
    "rimraf": "^2.6.2",
    "typescript": "3.1.6"
  }
}
